---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "k8s-dra-driver.fullname" . }}-role
  namespace: {{ include "k8s-dra-driver.namespace" . }}
rules:
- apiGroups:
  - ""
  - resource.k8s.io
  - gpu.resource.nvidia.com
  - nas.gpu.resource.nvidia.com
  resources: ["*"]
  verbs: ["*"]
